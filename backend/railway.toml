[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m app.create_db && python -m app.seed && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
